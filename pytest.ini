[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests as integration tests (require running infrastructure)
    unit: marks tests as unit tests (no external deps)
filterwarnings =
    ignore::DeprecationWarning
